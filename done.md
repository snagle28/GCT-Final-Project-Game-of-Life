# 오늘 한 일 (쉬운 설명)

## 1. 뒤로가기 버튼 — 환경(Environment) 다시 고르기

**문제:** Forest/Ocean(Soundscape1/2)에서 플레이하다가 다른 환경으로 바꾸려면
방법이 없었음.

**한 일:**
- 화면 **오른쪽 아래에 ◄ (뒤로가기) 버튼**을 자동으로 띄움.
- 누르면 **"Select a Soundscape Environment"(환경 선택) 화면으로 바로** 돌아감.
- 원래는 돌아가면 인트로("CONWAY'S SYMPHONY" + "click to begin")가 다시 나왔는데,
  그 단계를 **건너뛰고 선택 화면이 곧장** 뜨도록 처리.

**중간에 고친 것들:**
- 선택 화면이 사실은 `Intro Sound` 씬 안의 타임라인 중간 구간이라는 걸 찾아내서,
  그 지점으로 점프하게 만듦.
- 동기화가 만든 중복 파일(`SceneManagement 2.cs`) 때문에 컴파일이 안 되던 것 삭제.
- 버튼 크기를 보기 좋게 줄임(56×56).

---

## 2. 소리가 "뚝뚝 끊기는(discrete)" 문제 → 부드럽게

**문제:** 멜로디가 세대마다 같은 음을 처음부터 계속 다시 때려서
"쾅-쾅-쾅" 박자처럼 들리고 딱딱했음.

**한 일 (3단계로 발전):**
1. 처음엔 음을 겹쳐 울리게(서스테인 페달 느낌) + "페달 지속 시간" 조절 추가.
2. 그래도 "매번 다시 때리는" 느낌이 남아서, **레가토 방식**으로 바꿈:
   - 음이 **바뀔 때만** 새로 치고, 안 바뀌면 그냥 **유지**(다시 안 때림).
   - 바뀔 땐 **이전 음과 부드럽게 겹쳐 넘어감**(크로스페이드).
   - 유지 중인 음은 보드가 옆으로 움직이면 **좌우(스테레오)만 따라 이동**.
   - 새 음 시작을 **둥글게(fade-in)** 해서 날카로운 어택 제거.
3. 결과적으로 끊김이 크게 줄고, **슬라이더로 부드러움 정도를 조절**할 수 있게 됨.

---

## 3. 음(소리) 매핑을 쉽게 바꿀 수 있게

- 각 색의 Y축(위/아래)에 어떤 소리가 나는지를 **파일만 바꿔서 쉽게 변경** 가능하게 함.
- 색당 음 **개수도 고정(12)이 아니라, 폴더에 넣은 파일 개수대로 자동** 결정.
  (색마다 개수가 달라도 됨. grid 행 수는 넘지 않게 자동 제한.)

---

## 4. 문서 정리

- `docs/sonification-tuning.md` — 소리 관련 **조절 변수 + 기본값** 전체 정리.
- `audio_guide.md` — **오디오팀용**: 소리 교체 / 파라미터 변경 방법 안내.

---

## 건드린 파일

- `Assets/SceneManagement.cs` — 뒤로가기/인트로 스킵
- `Assets/Sonification/BackButton.cs` — 뒤로가기 버튼 (신규)
- `Assets/Sonification/MelodyPlayer.cs` — 레가토 멜로디 + 자동 음 개수
- `Assets/GameManager.cs` — 버튼/멜로디 파라미터 연결
- `Assets/Resources/UI/BackButtonIcon.prefab` — 버튼 아이콘 (신규)
- 문서: `docs/sonification-tuning.md`, `audio_guide.md`, `done.md`
