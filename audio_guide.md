# 오디오팀 가이드 — 소리 교체 & 파라미터 변경

이 게임의 소리는 5개 레이어(AD1~AD5)로 되어 있습니다. **코드를 몰라도** 소리 파일을 바꾸고
값을 조절할 수 있도록 정리했습니다.

## 기본 개념 2가지

1. **환경(Environment) 2개**
   - **Forest** = 씬 `Soundscape1` = `env1`
   - **Ocean** = 씬 `Soundscape2` = `env2`
   - 소리 폴더가 `Forest` / `Ocean`으로 나뉘어 있고, 게임이 씬에 맞는 폴더를 자동으로 씁니다.

2. **소리는 `Assets/Resources/...` 폴더에 넣으면 자동 인식**
   - 폴더/파일명 규칙만 지키면 됩니다. Unity Inspector에서 일일이 연결할 필요 없음.
   - 지원 포맷: `.wav`, `.mp3` 모두 가능.

---

# 1) 소리 교체하는 법 (레이어별)

| 레이어 | 무슨 소리 | 폴더 | 파일 규칙 |
|---|---|---|---|
| **AD1/AD2 멜로디** | 세대마다 색별로 나는 음(피아노 등) | `Assets/Resources/Notes/{Forest,Ocean}/` | `{Env}_Color{색}_{번호}` (아래 상세) |
| **AD3 클릭음** | 셀을 그릴 때 나는 효과음 | `Assets/Resources/Footsteps/{Forest,Ocean}/` | 아무 이름. 여러 개 넣으면 무작위 재생 |
| **AD4 시작/정지음** | 스페이스바로 재생/정지할 때 | `Assets/Resources/StartSounds/{Forest,Ocean}/` | 파일 1개 (이름 자유) |
| **AD5 배경 루프** | 계속 깔리는 배경 사운드 | `Assets/Resources/Soundscapes/{Forest,Ocean}/` | 파일 1개 (이름 자유, 루프됨) |

> `{Env}`는 `Forest` 또는 `Ocean`. 예: `Forest_Color1_1`, `Ocean_Color3_8`.

### 교체 방법 (공통)
1. 해당 폴더에 새 오디오 파일을 넣습니다 (기존 파일을 덮어쓰거나, 새 파일로 교체).
2. 멜로디(AD1/AD2)만 **이름 규칙**이 중요합니다. 나머지(AD3/4/5)는 이름 자유.
3. Unity를 켜두면 자동으로 인식됩니다.

---

# 2) ⭐ 멜로디(AD1/AD2) 음 매핑 — 가장 중요

각 색(Color1·2·3)마다, 셀들의 **평균 높이(Y)** 에 따라 음이 정해집니다.
**아래쪽 = 낮은 음, 위쪽 = 높은 음.**

### 파일 이름 규칙
```
{Env}_Color{색}_{번호}
예) Forest_Color1_1   ← Forest, 1번 색, 가장 낮은 음
    Forest_Color1_2   ← 그 다음 음
    ...
    Forest_Color1_12  ← 가장 높은 음
```
- **번호 `_1`이 제일 낮은 음**, 번호가 커질수록 높은 음.
- **색당 음 개수는 자유** — 폴더에 넣은 개수만큼 자동으로 Y축에 펼쳐집니다.
  - 예: Color1을 8개만 넣으면 8단계, Color2를 16개 넣으면 16단계. 색마다 달라도 됨.
  - (단, 한 색의 개수가 격자 행 수 50을 넘으면 50으로 제한됩니다 — 그 이상은 의미 없음.)

### 바꾸고 싶을 때
- **특정 Y에 다른 소리** → 그 번호 파일을 교체. (예: 위에서 3번째 음 바꾸려면 `_10`쯤 교체)
- **음을 더 촘촘하게/성기게** → 파일을 더 넣거나 덜 넣기.
- **색마다 완전히 다른 악기** → 각 `Color{색}` 파일들을 원하는 소리로 채우기.
- **Forest와 Ocean 다른 음색** → 각 폴더에 따로 넣기 (이미 분리되어 있음).

### 멜로디 음원 특성 (권장)
- 한 음 = **하나의 one-shot 샘플** (어택 → 자연 감쇠 → 무음 꼬리).
- 길이는 자유지만 현재는 약 4초짜리를 기준으로 동작합니다.
- 인위적 페이드는 코드가 처리하니, **샘플 자체는 자연스러운 음 하나**면 됩니다.

---

# 3) 파라미터(소리 느낌) 변경하는 법

### 어디서?
Unity 에디터에서 씬(`Soundscape1` 또는 `Soundscape2`)을 열고 →
Hierarchy에서 **`GameManager`** 오브젝트 선택 → Inspector의 **"Sonification"** 섹션.

> **Play(재생) 중에도 슬라이더를 움직이면 즉시 적용**됩니다. 듣고 맞춰보세요.
> (단, Play 중 바꾼 값은 저장 안 됨 — 멈춘 상태에서 바꿔야 저장됩니다.)

### 멜로디 (AD1/AD2)

| 슬라이더 | 무슨 역할 | 기본값 | 팁 |
|---|---|---|---|
| Melody Volume | 멜로디 크기 | 0.6 | |
| Melody Pan Strength | 좌우(스테레오) 벌어짐 | 2.5 | 클수록 좌우 극단적 |
| Melody Min Interval | 음 갱신 최소 간격(초) | 0.12 | 빠른 템포에서 음 뭉침 방지 |
| **Melody Sustain Seconds** | 음이 유지되는 시간 | 3.5 | **반복 간격 ≈ Sustain + Release** |
| **Melody Crossfade Seconds** | 음 바뀔 때 이어지는 길이 | 0.3 | **클수록 더 부드럽게** (0.4~0.6) |
| **Melody Attack Seconds** | 음 시작을 둥글게 | 0.08 | **클수록 어택 부드러움** (0.15~0.25) |
| Melody Release Seconds | 음 꼬리 페이드 길이 | 0.3 | |
| Melody Max Voices Per Color | 색당 동시 음 수 상한 | 6 | 너무 뭉개지면 줄이기 |

**"더 부드럽게" 하려면** → Crossfade ↑, Attack ↑, Sustain ↑.
**"같은 음 반복이 잦다" 하려면** → Sustain ↑ (최대 ~4초, 샘플 길이가 한계).

### 효과음 / 배경

| 슬라이더 | 역할 | 기본값 |
|---|---|---|
| Footstep Volume (AD3) | 셀 그릴 때 클릭음 크기 | 1.0 |
| Start Sound Volume (AD4) | 시작/정지음 크기 | 1.0 |
| Bg Min Volume (AD5) | 셀 0개일 때 배경 볼륨 | 0.1 |
| Bg Max Volume (AD5) | 셀 많을 때 배경 볼륨 | 0.8 |
| Bg Cap Fraction (AD5) | 전체 셀의 몇 %에서 최대 볼륨 | 0.3 |
| Bg Smoothing (AD5) | 배경 볼륨 변화 속도 | 0.1 (작을수록 천천히) |

---

# 4) 자주 묻는 질문

**Q. 파일을 바꿨는데 소리가 안 나와요.**
- 폴더 위치(`Assets/Resources/...` 안)와, 멜로디는 이름 규칙(`{Env}_Color{색}_{번호}`)을 확인하세요.
- Unity 콘솔에 `MelodyPlayer: no note clips...` 같은 경고가 있으면 이름/폴더 문제입니다.

**Q. 한 색만 소리가 안 나요.**
- 그 색의 파일(`..._Color2_...` 등)이 폴더에 있는지 확인.

**Q. Inspector에서 슬라이더가 안 보여요.**
- `GameManager` 오브젝트를 선택했는지, "Sonification" 섹션을 펼쳤는지 확인.
- 슬라이더는 게임을 한 번 실행(Play)하면 자동 생성된 컴포넌트에도 나타납니다.

**Q. 멜로디 음 개수를 바꾸고 싶어요.**
- 그냥 폴더에 파일을 더 넣거나 빼면 됩니다. 번호는 1부터 연속으로 매기는 걸 권장.

---

자세한 변수 레퍼런스(코드 위치 포함)는 `docs/sonification-tuning.md` 참고.
